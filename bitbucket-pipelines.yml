image: quay.io/zleblanc/bitbucket-builder:v2

pipelines:
  default:
    - step:
        name: 'Build execution environment'
        deployment: master
        script:
          - ansible-builder --help
